// map returns the new Array,
// it do not modify origional array
let arr = [1, 2, 3, 4, 5];
const newArr = arr.map((num) => num * num);

console.log(arr);
console.log(newArr);
// foreach do not return anything
